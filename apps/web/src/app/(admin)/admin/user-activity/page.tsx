"use client";
import { useQuery } from "@tanstack/react-query";
import { AlertTriangle, ArrowLeft, Search, ShieldCheck, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { userActivityApi } from "@/lib/api";
import { Input } from "@/components/ui/input";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

export default function UserActivityCenterPage() {
  const [selected,setSelected]=useState<string>(); const [from,setFrom]=useState(""); const [to,setTo]=useState(""); const [filter,setFilter]=useState<string>();
  const tree=useQuery({queryKey:["user-activity","tree"],queryFn:userActivityApi.tree}); const overview=useQuery({queryKey:["user-activity","overview",from,to],queryFn:()=>userActivityApi.overview(from||undefined,to||undefined)}); const timeline=useQuery({queryKey:["user-activity","timeline",selected,from,to],queryFn:()=>userActivityApi.timeline(selected!,from||undefined,to||undefined),enabled:!!selected});
  const affected=useMemo(()=> (overview.data?.affected??[]).filter((x:any)=>!filter||filter==="denied"?x.denied>0:filter==="alerts"?x.alerts>0:x.risk!=="NORMAL"),[overview.data,filter]);
  const users=tree.data??[]; const watch=overview.data?.affected??[];
  return <div className="space-y-5"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-semibold">مركز نشاط المستخدمين</h1><p className="text-muted-foreground">Operational activity by organizational scope.</p></div>{selected&&<Button variant="outline" onClick={()=>setSelected(undefined)}><ArrowLeft className="h-4 w-4"/> العودة للملخص العام</Button>}</div><div className="flex gap-2"><Input type="date" value={from} onChange={e=>setFrom(e.target.value)}/><Input type="date" value={to} onChange={e=>setTo(e.target.value)}/></div><div className="grid gap-5 xl:grid-cols-[minmax(520px,42%)_minmax(0,1fr)]"><Card><CardHeader><CardTitle>Group Tree</CardTitle></CardHeader><CardContent className="max-h-[70vh] space-y-3 overflow-auto">{watch.length>0&&<button className="w-full rounded bg-amber-500/15 p-3 text-left font-medium" onClick={()=>setFilter("watch")}>⚠️ يحتاج متابعة ({watch.length})</button>}{Object.entries(users.reduce((m:any,u:any)=>{const c=u.company?.name??"Platform",o=u.orgUnit?.name??"Unassigned",g=u.role?.code??"Other";m[c]??={};m[c][o]??={};m[c][o][g]??=[];m[c][o][g].push(u);return m},{})).map(([company,orgs]:any)=><details key={company} open><summary className="cursor-pointer font-semibold">{company}</summary>{Object.entries(orgs).map(([org,groups]:any)=><details key={org} open className="ml-3"><summary className="cursor-pointer">{org}</summary>{Object.entries(groups).map(([role,list]:any)=><details key={role} open className="ml-3"><summary className="cursor-pointer text-sm">{role} ({list.length})</summary>{list.map((u:any)=><button key={u.id} className={`ml-3 block w-[calc(100%-12px)] rounded p-2 text-left text-sm hover:bg-muted ${selected===u.id?"bg-primary text-primary-foreground":""}`} onClick={()=>setSelected(u.id)}>{u.fullName}<span className="ml-2 text-xs opacity-70">{u.email}</span></button>)}</details>)}</details>)}</details>)}</CardContent></Card><div className="min-w-0 space-y-4">{!selected?<><div className="grid gap-3 sm:grid-cols-3"><Metric title="Denied" value={`${overview.data?.denied?.accounts??0} accounts · ${overview.data?.denied?.total??0} attempts`} icon={AlertTriangle} onClick={()=>setFilter("denied")}/><Metric title="Security Alerts" value={`${overview.data?.securityAlerts?.accounts??0} accounts · ${overview.data?.securityAlerts?.total??0} alerts`} icon={ShieldCheck} onClick={()=>setFilter("alerts")}/><Metric title="Risk" value={`W ${overview.data?.risk?.WATCH??0} · S ${overview.data?.risk?.SUSPICIOUS??0} · H ${overview.data?.risk?.HIGH_RISK??0}`} icon={UsersRound} onClick={()=>setFilter("risk")}/></div><Card><CardHeader><CardTitle>Affected Accounts</CardTitle></CardHeader><CardContent className="space-y-2">{affected.map((u:any)=><button key={u.id} className="flex w-full justify-between rounded border p-3 text-left hover:bg-muted" onClick={()=>setSelected(u.id)}><span><b>{u.name}</b><span className="block text-xs text-muted-foreground">{u.email} · {u.company} · {u.branch} · {u.role}</span></span><b>{u.count}</b></button>)}{!affected.length&&<p className="text-muted-foreground">No affected accounts.</p>}</CardContent></Card></>:<><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{[["Total Events",timeline.data?.summary.totalEvents],["Business",timeline.data?.summary.businessEvents],["Denied",timeline.data?.summary.deniedEvents],["Risk",timeline.data?.summary.riskLevel]].map(([l,v])=><Card key={l as string}><CardContent className="p-4"><span className="text-sm text-muted-foreground">{l}</span><b className="ml-2">{v??"—"}</b></CardContent></Card>)}</div><Card><CardHeader><CardTitle>Timeline</CardTitle></CardHeader><CardContent className="space-y-3">{timeline.data?.items?.map((e:any)=><div key={e.id} className="border-l-2 pl-3"><b>{e.type}</b><p className="text-sm text-muted-foreground">{new Date(e.timestamp).toLocaleString()} · {e.outcome}</p></div>)}</CardContent></Card></>}</div></div></div>;
}
function Metric({title,value,icon:Icon,onClick}:{title:string;value:string;icon:any;onClick:()=>void}){return <button onClick={onClick} className="text-left"><Card><CardContent className="p-4"><Icon className="mb-2 h-5 w-5"/><p className="text-sm text-muted-foreground">{title}</p><b>{value}</b></CardContent></Card></button>}
