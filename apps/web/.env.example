NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Browser-only Maps JavaScript API key. Restrict it by HTTP referrer; never
# expose or reuse the server-side Google Places credential here.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
